@model BmiModel

<div class="container">
    <h2>BMI Hisoblash</h2>

    <div class="info-section mb-4">
        <p>
            Ushbu sayt sizga tana massasi indeksi (BMI) ni hisoblash imkonini beradi. BMI inson tanasidagi yog' miqdorini hisoblash uchun asosiy o'lchovlardan biri bo'lib, tana og'irligi va bo'yi o'rtasidagi bog'liqlikni ko'rsatadi. Ushbu ko'rsatkich orqali sog'ligingiz holatini kuzatishingiz mumkin.
        </p>
    </div>

    <form asp-action="Index" method="post">
        <div class="form-group">
            <label for="Weight">Vazn (kg):</label>
            <input type="number" id="Weight" asp-for="Weight" class="form-control" />
            <span asp-validation-for="Weight" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label for="Height">Bo'y (sm):</label>
            <input type="number" id="Height" asp-for="Height" class="form-control" />
            <span asp-validation-for="Height" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-primary">Hisoblash</button>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
